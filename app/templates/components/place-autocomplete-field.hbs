{{#if hasBlock}}
  {{yield this}}
{{else}}
    {{md-input
        id=id
        value=value
        label=label
        types=geoTypes
        width="width: 100%"
        validate=validate
        errors=errors
        icon=icon}}
{{/if}}
{{yield}}
