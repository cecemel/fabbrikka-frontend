{{#each rows as |row|}}
	<div class="row">
    		{{#each row as |rowItem|}}
    		<div class="col s12 m6 l3">
      			{{component itemComponentName item=rowItem onClickLinkTo=onClickLinkTo}}
      		</div>
   			{{/each}}
	</div>
{{/each}}
