
import { equalStrings } from 'fabbrikka-frontend/helpers/equal-strings';
import { module, test } from 'qunit';

module('Unit | Helper | equal strings');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = equalStrings([42]);
  assert.ok(result);
});

